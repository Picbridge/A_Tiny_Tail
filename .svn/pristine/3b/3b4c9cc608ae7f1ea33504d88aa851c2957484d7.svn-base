using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.SceneManagement;

public class Level
{
    public int id;
    public int map_height;
    public int map_width;
    public string levelName;

    public Tile[,] tile_map;

    public Level(int id_)
    {
        id = id_;
        map_height = 0;
        map_width = 0;
    }

    public void TileSetting()
    {

        if (map_height == 0 || map_width == 0)
        {
            Debug.Log("HEIGHT OR WIDTH OF MAP IS NOT SET!");
        }
        else
        {
            tile_map = new Tile[map_height + 1, map_width + 1];

            for (int i = 0; i < map_height + 1; i++)
            {
                for (int j = 0; j < map_width + 1; j++)
                {
                    tile_map[i, j] = new Tile();
                }
            }
        }
    }
}

public class Tile
{
    public bool isGoingLeft;
    public bool isGoingUp;
    public bool isGoingRight;
    public bool isGoingDown;
}



public class TileMapManager : Singleton<TileMapManager>
{
    public FileReader fileReader;
    public int levelSize = 11;

    public Level[] level;
    public float tilePortion;

    void Start ()
    {        
        level = new Level[levelSize];        
        
        for(int i = 0; i < levelSize; i++)
            level[i] = new Level(i);

        level[0].levelName = "H_Tutorial.txt";
        level[1].levelName = "H_Lv1.txt";
        level[2].levelName = "H_Lv2.txt";
        level[3].levelName = "C_Tutorial.txt";
        level[4].levelName = "C_Lv2.txt";
        level[5].levelName = "L_Tutorial.txt";
        level[6].levelName = "L_Lv1.txt";
        level[7].levelName = "L_Lv2.txt";
        level[8].levelName = "M_Lv1.txt";
        level[9].levelName = "M_Lv2.txt";
        level[10].levelName = "M_Lv3.txt";
        

        tilePortion = 1.0f;

        for (int i = 0; i < levelSize; i++)
        {
            fileReader.ReadLevel(level[i]);
        }        
    }

    private void Update()
    {        
        //치트 코드
        if (Input.GetKeyDown(KeyCode.Keypad0) && GameSceneIndexManager.instance.currentSceneIndex <= levelSize)
        {
            Time.timeScale = 1.0f;
            GameSceneIndexManager.instance.currentSceneIndex = 0;
            GameSceneIndexManager.instance.currentLevelIndex = 0;
            SceneManager.LoadSceneAsync(0);
        }

        if (Input.GetKeyDown(KeyCode.Keypad1) && GameSceneIndexManager.instance.currentSceneIndex <= levelSize)
        {
            Time.timeScale = 1.0f;
            GameSceneIndexManager.instance.currentSceneIndex = 1;
            GameSceneIndexManager.instance.currentLevelIndex = 0;
            SceneManager.LoadSceneAsync(1);
        }

        if (Input.GetKeyDown(KeyCode.Keypad2) && GameSceneIndexManager.instance.currentSceneIndex <= levelSize)
        {
            Time.timeScale = 1.0f;
            GameSceneIndexManager.instance.currentSceneIndex = 2;
            GameSceneIndexManager.instance.currentLevelIndex = 1;
            SceneManager.LoadSceneAsync(2);
        }

        if (Input.GetKeyDown(KeyCode.Keypad3) && GameSceneIndexManager.instance.currentSceneIndex <= levelSize)
        {
            Time.timeScale = 1.0f;
            GameSceneIndexManager.instance.currentSceneIndex = 3;
            GameSceneIndexManager.instance.currentLevelIndex = 2;
            SceneManager.LoadScene(3);
        }

        if (Input.GetKeyDown(KeyCode.Keypad4) && GameSceneIndexManager.instance.currentSceneIndex <= levelSize)
        {
            Time.timeScale = 1.0f;
            GameSceneIndexManager.instance.currentSceneIndex = 4;
            GameSceneIndexManager.instance.currentLevelIndex = 3;
            SceneManager.LoadScene(4);
        }

        if (Input.GetKeyDown(KeyCode.Keypad5) && GameSceneIndexManager.instance.currentSceneIndex <= levelSize)
        {
            Time.timeScale = 1.0f;
            GameSceneIndexManager.instance.currentSceneIndex = 5;
            GameSceneIndexManager.instance.currentLevelIndex = 4;
            SceneManager.LoadScene(5);
        }

        if (Input.GetKeyDown(KeyCode.Keypad6) && GameSceneIndexManager.instance.currentSceneIndex <= levelSize)
        {
            Time.timeScale = 1.0f;
            GameSceneIndexManager.instance.currentSceneIndex = 6;
            GameSceneIndexManager.instance.currentLevelIndex = 5;
            SceneManager.LoadScene(6);
        }

        if (Input.GetKeyDown(KeyCode.Keypad7) && GameSceneIndexManager.instance.currentSceneIndex <= levelSize)
        {
            Time.timeScale = 1.0f;
            GameSceneIndexManager.instance.currentSceneIndex = 7;
            GameSceneIndexManager.instance.currentLevelIndex = 6;
            SceneManager.LoadScene(7);
        }

        if (Input.GetKeyDown(KeyCode.Keypad8) && GameSceneIndexManager.instance.currentSceneIndex <= levelSize)
        {
            Time.timeScale = 1.0f;
            GameSceneIndexManager.instance.currentSceneIndex = 8;
            GameSceneIndexManager.instance.currentLevelIndex = 7;
            SceneManager.LoadScene(8);
        }

        if (Input.GetKeyDown(KeyCode.Keypad9) && GameSceneIndexManager.instance.currentSceneIndex <= levelSize)
        {
            Time.timeScale = 1.0f;
            GameSceneIndexManager.instance.currentSceneIndex = 9;
            GameSceneIndexManager.instance.currentLevelIndex = 8;
            SceneManager.LoadScene(9);
        }

        if (Input.GetKeyDown(KeyCode.KeypadDivide) && GameSceneIndexManager.instance.currentSceneIndex <= levelSize)
        {
            Time.timeScale = 1.0f;
            GameSceneIndexManager.instance.currentSceneIndex = 10;
            GameSceneIndexManager.instance.currentLevelIndex = 9;
            SceneManager.LoadScene(10);
        }

        if (Input.GetKeyDown(KeyCode.KeypadMultiply) && GameSceneIndexManager.instance.currentSceneIndex <= levelSize)
        {
            Time.timeScale = 1.0f;
            GameSceneIndexManager.instance.currentSceneIndex = 11;
            GameSceneIndexManager.instance.currentLevelIndex = 10;
            SceneManager.LoadScene(11);
        }
    }

}
